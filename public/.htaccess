RewriteEngine On

# Try cached page
RewriteRule ^([^.]+)$ $1.html [QSA]

# When the file does not exist send it to Chickenwire
RewriteCond %{REQUEST_FILENAME} !-f 	
RewriteRule ^(.*)$ index.php [QSA,L]

php_flag magic_quotes_gpc off
php_flag register_globals off
